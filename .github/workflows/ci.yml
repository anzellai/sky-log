name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "stable"

      - name: Install goimports (required by Sky FFI codegen)
        run: go install golang.org/x/tools/cmd/goimports@latest

      - name: Install Sky compiler
        run: |
          mkdir -p "$HOME/.local/bin"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          curl -fsSL https://raw.githubusercontent.com/anzellai/sky/main/install.sh | SKY_INSTALL_DIR="$HOME/.local/bin" sh
          "$HOME/.local/bin/sky" --version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: sky install

      - name: Build
        run: sky build src/Main.sky

      - name: Verify binary
        run: |
          test -x ./sky-out/app && file ./sky-out/app
          ./sky-out/app --help | head -3
